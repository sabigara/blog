export const CONTAINER_WIDTH = "48rem" as const;
export const CONTAINER_PADDING = "1rem" as const;
export const IMG_SIZES =
  `sizes="(max-width: ${CONTAINER_WIDTH}) 100vw, ${CONTAINER_WIDTH}` as const;
